[workspace]
changelog_config = "cliff.toml"
dependencies_update = true
pr_labels = ["release"]
# Handled by the publish workflow's "Create GitHub release" step
git_release_enable = false
# Enable after the first release to crates.io
semver_check = false
